FROM python:3.12-slim

WORKDIR /app

# 只复制 webhook 目录及其依赖
COPY webhook/ /app/webhook/

# 使用 webhook 目录下的 requirements.txt
RUN pip install -r webhook/requirements.txt

# 设置 Python 路径
ENV PYTHONPATH=/app

EXPOSE 8000

CMD ["uvicorn", "webhook.webhook:app", "--host", "0.0.0.0", "--port", "8000"] 